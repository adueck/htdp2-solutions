;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-beginner-reader.ss" "lang")((modname ex-117) (read-case-sensitive #t) (teachpacks ()) (htdp-settings #(#t constructor repeating-decimal #f #t none #f () #f)))
;(3 + 4)

; illegal way of making a compound
; after a ( you need to have either define, a primitive,
; a variable, or condition

; number?

; primitives or function applications must be preceeded
; by (

; (x)

;  (variable must be followed by at least one expr