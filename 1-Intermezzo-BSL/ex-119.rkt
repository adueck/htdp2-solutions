;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-beginner-reader.ss" "lang")((modname ex-119) (read-case-sensitive #t) (teachpacks ()) (htdp-settings #(#t constructor repeating-decimal #f #t none #f () #f)))
;(define (f "x") x)

; in (define (var var ...) expr) an "x" is used instead of
; a variable. "x" is an expression and not allowed

;(define (f x y z) (x))

; in (define (var var ...) expr) the (x) used is not a valid
; expression
